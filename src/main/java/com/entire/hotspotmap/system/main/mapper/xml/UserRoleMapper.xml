<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.entire.hotspotmap.system.main.mapper.UserRoleMapper">

    <insert id="insertBatch">
        INSERT INTO user_role(user_id, role_id) VALUES
        <foreach collection="roleIds" item="roleId" separator=",">
            (#{userId}, #{roleId})
        </foreach>
    </insert>

    <select id="selectByUserId" resultType="com.entire.hotspotmap.system.main.entity.UserRole">
        SELECT *
        FROM role
        WHERE role_id IN
        (
            SELECT role_id
            FROM user_role
            WHERE user_id = #{userId}
        )
    </select>

    <select id="selectByUserIds" resultType="com.entire.hotspotmap.system.main.entity.UserRole">
        SELECT a.user_id, b.*
        FROM user_role a
        LEFT JOIN role b ON a.role_id = b.role_id
        WHERE a.user_id IN
        <foreach collection="userIds" open="(" close=")" separator="," item="userId">
            #{userId}
        </foreach>
    </select>

</mapper>